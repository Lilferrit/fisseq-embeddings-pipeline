# API: config

Shared Hydra structured config base classes -- see
[Architecture](../architecture.md) for the design behind `random_seed`
being defined once here rather than per-stage.

::: fisseq_embeddings_pipeline.config.app

::: fisseq_embeddings_pipeline.config.input
