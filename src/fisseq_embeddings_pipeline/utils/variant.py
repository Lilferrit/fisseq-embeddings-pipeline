"""Vendor unchanged from fisseq-data-pipeline's src/fisseq_data_pipeline/utils/variant.py
(SPEC.md §3 decision 2) -- classify_variant() / variant_classification(),
used throughout (FILTER_EMBEDDINGS §6.4, GLOBAL_VARIANT_DISTINGUISHABILITY §6.8)
to mark synonymous rows as the control population (meta_is_control).

TODO(Epic 1, part of scaffolding): vendor unchanged.
"""
