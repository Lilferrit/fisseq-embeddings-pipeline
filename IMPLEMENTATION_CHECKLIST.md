# Implementation Checklist

An Agile-style backlog for implementing `SPEC.md` in `fisseq-embeddings-pipeline`. Work through the epics in order — later epics assume earlier ones exist. Each story lists concrete, verifiable acceptance criteria (a file, a column set, a passing test) rather than vague done-ness; check items off (`- [x]`) as you complete them and commit the updated checklist alongside the code, so it stays a true record of progress.

Every story cites the `SPEC.md` section it implements — read that section (and, where noted, the real vendored file in `fisseq-data-pipeline`) before writing code; the code blocks in `SPEC.md` are illustrative sketches, not guaranteed-correct source to copy verbatim.

**Suggested sequencing** (four increments, each independently demoable):

- **M1 — Foundations**: Epic 0, Epic 1, Epic 2
- **M2 — Embedding & scoring core**: Epic 3, Epic 4, Epic 5, Epic 6
- **M3 — Global stages & orchestration**: Epic 7, Epic 8, Epic 9
- **M4 — Hardening & handoff**: Epic 10, Epic 11, Epic 12

---

## Epic 0 — Repo & shared foundations

*Goal: every later epic has a working `AppConfig`, vendored utilities, and a package that imports cleanly.*

### Story 0.1 — Vendor shared utilities from `fisseq-data-pipeline`
- [x] `utils/constants.py`: vendored unchanged (`CONTROL_COLUMN`/`CONTROL_COLUMN_NAME`, `FEATURE_SELECTOR`, `META_SELECTOR`, `EPS`, `META_BATCH_COL`, `META_BARCODE_COL`, `META_EDIT_DISTANCE_COL`, etc.) **plus** a new `EMBEDDING_SELECTOR = cs.matches(r"^emb_\d+$")` (SPEC.md §6.3's Output note).
- [x] `utils/variant.py`: vendored unchanged (`classify_variant`). **Checklist correction**: `variant_classification` does not live in the real source repo's `utils/variant.py` — it's defined in `aggregate.py` and will be ported alongside that file in Epic 4/5, not here.
- [x] `utils/batches.py`: vendored unchanged (`load_batches`).
- [x] `utils/log.py`: vendored unchanged (`setup_logging`).
- [x] `utils/xgbparams.py`: vendored, **with exactly one line changed** — `train_binary_xgboost`'s `params["seed"] = cfg.random_state` becomes `params["seed"] = cfg.random_seed` (SPEC.md §6.6's Seed note). Everything else (`XGBoostParams`, `XGBoostConfig`, `get_dmatrix`, `get_dmatrix_multiclass`, `resolve_feature_importance`, `split_indices_stratified`, `evaluate_binary`) unchanged.
- [x] `utils/dimreduction.py`: vendored, **with one added parameter** — `compute_pca` gains `random_state: int = 0`, threaded into its internal `PCA(...)` call instead of the hardcoded `0` (SPEC.md §6.7's Seed note).
- [x] `config/input.py`: vendored unchanged (`InputConfig`, `LabeledInputConfig`). `config/__init__.py` populated to export `AppConfig`/`InputConfig`/`LabeledInputConfig`, matching the source repo's, since `utils/log.py`'s `setup_logging` imports `AppConfig` from `..config`.
- [x] Unit test per vendored-with-changes file confirming the deviation behaves as documented (e.g. `xgbparams`'s `train_binary_xgboost` actually reads `cfg.random_seed`, not `cfg.random_state`).

### Story 0.2 — `AppConfig` with the shared seed
- [x] `config/app.py` implements `AppConfig` per SPEC.md §3's code block: `output_dir`, `output_root`, `log_level`, `random_seed: int = 0`.
- [x] Every stage config in later epics extends `AppConfig` — no stage adds its own `random_state`/seed field (SPEC.md §3 decision 11).
- [x] Unit test: a `ConfigStore`-registered stage config's default `random_seed` is `0` and is overridable via Hydra CLI override syntax.

### Story 0.3 — Package/build sanity
- [x] `uv sync --group dev` succeeds from a clean clone.
- [x] `python -c "import fisseq_embeddings_pipeline"` succeeds.
- [x] `uv run ruff check .` and `uv run pre-commit run --all-files` pass on the scaffold as-is.
- [x] CI (or at minimum a documented local command) runs `pytest tests/unit` and `ruff check` on every push — decide and document where (GitHub Actions, matching `fisseq-data-pipeline`'s `.github/`, or elsewhere). **Decided:** `.github/workflows/ci.yml`, mirroring `fisseq-data-pipeline`'s `.github/workflows/pr-checks.yml` shape (`astral-sh/setup-uv`, `uv sync --group dev`) with `lint`/`unit-tests` jobs against `master` (this repo's actual default branch). No `integration-tests` job yet — `tests/integration` has no content until Epic 11; add one then, mirroring that same workflow's Java+Nextflow setup.

---

## Epic 1 — Cell Dataset: `BUILD_DATASET` (SPEC.md §6.1)

*Goal: crop one experiment's cells directly from `starcall-workflow`'s stitched phenotype images into a WebDataset, unconditionally (no QC gating). Stories 1.1–1.3 below describe the original design (reading `make_cell_images`'s pre-cropped output); Story 1.4 supersedes that with `BUILD_DATASET` doing its own cropping, since `make_cell_images` isn't reliably run for every experiment — see Story 1.4 for why.*

### Story 1.1 — Config & tile discovery
- [x] `BuildDatasetConfig(AppConfig)` implemented with every field from SPEC.md §6.1's dataclass (`phenotyping_dir`, `wells`, `grid_size`, `segmentation_type="cells"`, `window`, `shard_maxcount=2000`, `batch_stem`, `barcode_col_name="upBarcode"`, `aa_changes_col_name="aaChanges"`, `edit_distance_col_name="editDistance"`).
- [x] `discover_tiles()` globs `starcall-workflow`'s `{well}_grid{grid_size}/tile{x}x{y}y/` layout (**verified against a real `starcall-workflow` `origin/devel` checkout**, now mounted read-only: `workflow/rules/stitching.smk`'s `rule stitch_tile_pt` writes `'{well}_grid{grid_size}/tile{x}x{y}y/{corrected}_pt.tif'`, confirming the glob pattern). One correction versus SPEC.md's own sketch: rows are sorted by `(well, int(tile_x), int(tile_y))` instead of lexical `sorted(glob.glob(...))` order, since lexical sort misorders double-digit tile indices (`tile10x0y` would sort before `tile2x0y`).
- [x] Unit test: `discover_tiles()` against a small synthetic directory tree returns the expected `(well, tile, cell_table_csv, cell_crops_tif, mask_crops_tif)` rows, sorted deterministically (including a case with >10 tiles to actually exercise the numeric-sort fix above).

### Story 1.2 — Shard writing
- [x] `write_dataset_shards()` writes one WebDataset sample per cell (`crop.npy`, `mask.npy`, `meta.json` with `meta_batch`/`meta_well`/`meta_tile`/`meta_cell_index`/`meta_barcode`/`meta_aa_changes`/`meta_edit_distance`), keyed `"{well}_{tile}_{cell_index}"`, via `webdataset.ShardWriter(maxcount=cfg.shard_maxcount)`.
- [x] Empty-tile CSVs are skipped without erroring (matches `make_cell_images`'s empty-tile behavior). **Correction versus SPEC.md's sketch**: a real empty tile is a genuinely 0-byte file (`os.system('touch ...')` in the real `starcall-workflow` rule), which makes `pandas.read_csv` raise `EmptyDataError` rather than return a 0-row frame — SPEC.md's sketch only guards `len(table.index) == 0`, which alone only catches a header-only CSV. `write_dataset_shards()` catches both cases.
- [x] `metadata.parquet` is written alongside the shards with the same per-cell `meta_*` fields, no image data — confirm `QC_FILTER` (Epic 2) never needs to touch the `.tar` shards.
- [x] Hydra `main()` entry point wired up; `python -m fisseq_embeddings_pipeline.dataset <overrides>` runs end to end against a small synthetic `phenotyping_dir`.
- [x] Unit test: round-trip a tiny synthetic tile (2-3 cells) through `write_dataset_shards()` and confirm the shard's decoded samples and `metadata.parquet` rows match.

### Story 1.3 — Shard sizing sanity (SPEC.md §10, item 2)
- [x] Measure real per-sample byte size (crop + mask, your actual `window`/channel count) and confirm `shard_maxcount=2000` still lands shards in a reasonable size band (~hundreds of MB to ~1GB); adjust the default if not. **No real experiment data was available in this environment** (only the pipeline source repos are mounted) — computed an estimate instead from real defaults elsewhere in the stack (`starcall-workflow`'s 4-channel `phenotyping_channels` default, Cell-DINO's `224` crop size, assumed `uint16` crops): ≈440 KB/sample × `shard_maxcount=2000` ≈ ~880 MB/shard, within the target band, so the `2000` default is kept. Flagged as needing re-verification once real experiment data is available.
- [x] Document the measured number in `docs/configuration.md` (replacing the placeholder).

### Story 1.4 — Revision: crop from `stitch_tile_pt` instead of depending on `make_cell_images` (SPEC.md §6.1 revision)

*Why: per Alyssa La Fleur (starcall-workflow maintainer, Slack), `make_cell_images` (`phenotyping.smk`) is not reliably run for every experiment. Reading its real `origin/devel` source confirmed a likely cause: it reads `cell_table['xpos']`/`['ypos']` columns that don't exist in the real cell-table schema (`tabulate_cells` only ever produces `bbox_x1/y1/x2/y2`) and would raise `KeyError` against real data. `rule stitch_tile_pt` (`stitching.smk`) and `rule stitch_tile_from_well_segmentation` (`segmentation.smk`), by contrast, are always produced regardless of cell count — every experiment needs stitching/segmentation. This story replaces `BUILD_DATASET`'s dependency on `make_cell_images`'s output with `BUILD_DATASET` doing its own cropping, porting `make_cell_images`'s crop-window algorithm directly.*

- [x] `BuildDatasetConfig` gains `use_corrected: bool = False` (selects `corrected_pt.tif`/`raw_pt.tif`, mirroring starcall-workflow's own `config.phenotyping.use_corrected` default). `discover_tiles()` returns `pt_tif`/`mask_tif` columns pointing at `stitch_tile_pt`'s `{raw|corrected}_pt.tif` and `stitch_tile_from_well_segmentation`'s `{segmentation_type}_mask.tif`, replacing the old `cell_crops_tif`/`mask_crops_tif` columns (`make_cell_images`'s `*_crops_{window}.tif` outputs). `cfg.window` no longer affects `discover_tiles()` at all — only crop time.
- [x] `write_dataset_shards()` computes each cell's crop center as the integer midpoint of `bbox_x1/x2` and `bbox_y1/y2` (bbox columns are fixed structural constants, not new config fields — see SPEC.md §5.1's verified `xpos`/`ypos` correction), and crops via a new `_crop_cell()` helper ported from `make_cell_images`'s algorithm, including its positional `mask == i + 1` labeling convention (carried over from upstream as-is — flagged as an open risk, not a validated-correct choice; see below).
- [x] Empty-tile handling simplified: the old `pd.errors.EmptyDataError` branch (modeling `make_cell_images`'s own `touch`-emptied output) is removed — `tabulate_cells` always writes at least a header row, so only `len(table.index) == 0` needs guarding.
- [x] `image.ndim == 3` is normalized to 4D before flattening (`image[None]`), defending against the single-phenotype-cycle TIFF-squeeze ambiguity `make_cell_images` itself never guarded against.
- [x] `tests/unit/test_dataset.py` rewritten: fixtures now write a deterministic stitched tile image + label mask + bbox-columned cell table (position-encoding pixel content, so expected crops are computable via an independent pad-based oracle, not by re-running the code under test) instead of pre-cropped per-cell TIFFs. New tests cover centered/interior crops, edge-clipped crops (low and high sides), the positional-label-vs-cell_index regression case (non-sequential cell ids), multi-cycle/multi-channel cycle-major flattening, and the 3D-squeeze `ndim` guard.
- [x] SPEC.md §5.1 (drops `xpos`/`ypos`, documents the bbox-midpoint centroid and the verified schema gap), §5.2 (`stitch_tile_pt`/`stitch_tile_from_well_segmentation` promoted to what `BUILD_DATASET` reads; `make_cell_images` demoted to "algorithm ported, not output consumed"), §5.3, and §6.1 (config/`discover_tiles`/`write_dataset_shards` sketches, empty-tile note, output table) updated to match. `docs/configuration.md`'s shard-sizing note updated with a multi-cycle caveat and corrected dtype provenance.

**Open risks carried forward, not resolved by this story** (no real experiment data was available to validate against in this environment):
1. The positional `mask == i + 1` labeling assumes contiguous `1..N` mask labels in table row order — an assumption `make_cell_images` itself apparently never validated against real data either. Worth spot-checking against one real tile once available.
2. `stitch_tile_pt` and `stitch_tile_from_well_segmentation` are assumed to write into the same `phenotyping_dir` root (verified consistent with the real rule signatures) — worth confirming against an actual deployment's layout; add a separate `segmentation_dir` config field later if they ever diverge.

---

## Epic 2 — QC Filtering: `QC_FILTER` (SPEC.md §6.2)

*Goal: near-verbatim port of `qcfilter.py`, retargeted to read `BUILD_DATASET`'s `metadata.parquet`.*

### Story 2.1 — Port `qcfilter.py`
- [x] `QcFilterConfig` fields match SPEC.md's list (`bc_threshold=10`, `variant_bc_threshold=4`, `edit_distance_threshold=1`) with `downsample_amounts`/`downsample_classes`/`downsample_seed` **omitted** (SPEC.md §6.2's Resolved note — deliberately dropped, not an oversight).
- [x] `cell_files` input accepts `BUILD_DATASET`'s `metadata.parquet` directly (no separate CSV-reading path from the old `input.py`).
- [x] Outputs `filtered_cells.parquet` (composite join key + `meta_*` for QC-passed cells — a key list, not a copy), `barcode_counts.parquet`, `variants_per_barcode.parquet`, matching `fisseq-data-pipeline`'s existing contract.
- [x] Unit tests ported/adapted from `fisseq-data-pipeline/tests/unit/test_qcfilter.py`, updated for the `metadata.parquet` input path.

**Two deviations from the vendored source, flagged during implementation (beyond the
Resolved-note downsample drop above):**
1. `barcode_col_name`/`aa_changes_col_name`/`edit_distance_col_name` default to
   `"meta_barcode"`/`"meta_aa_changes"`/`"meta_edit_distance"` instead of the upstream raw-CSV
   names (`"upBarcode"`/`"aaChanges"`/`"editDistance"`) — `metadata.parquet` (Epic 1) already
   writes those columns under their canonical `meta_*` names, so this pipeline's only real
   `cell_files` input is never the raw, unrenamed cell table `filter_columns`'s rename step was
   originally written against. `filter_columns` itself is otherwise unchanged (renaming a column
   to its own existing name is a harmless no-op in Polars).
2. `select_variants`'s `mode="random"` seed now comes from `AppConfig.random_seed` (SPEC.md §3
   decision 11) instead of the dropped `downsample_seed` field, since no stage config is meant
   to add its own seed field.

Also fixed in passing: `read_file`'s upstream `if`/`elif` on file suffix has no `else`, so an
unrecognized suffix raised an opaque `UnboundLocalError` deep inside a later `.collect()` call —
the port raises a clear `ValueError` instead.

`modules/local/qc_filter.nf`'s CLI arg names (`barcode_count_threshold`/
`variant_barcode_count_threshold` vs. the real field names `bc_threshold`/`variant_bc_threshold`)
remain unreconciled — left for Epic 9 / Story 9.2, matching `build_dataset.nf`'s own still-open
`TODO(Epic 9)` even after Epic 1 shipped.

---

## Epic 3 — Cell Embeddings: `EMBED_CELLS` (SPEC.md §6.3)

*Goal: stream every cell through Cell-DINO in bag-of-channels mode. This is the epic SPEC.md flags as resting most heavily on assumptions — budget real time to read `dinov2` source before trusting any of its sketch.*

### Story 3.1 — Verify Cell-DINO's real inference API (SPEC.md §10, item 1 — do this first)
- [x] Read `dinov2/eval/setup.py` and `dinov2/models/vision_transformer.py` against the real `facebookresearch/dinov2` source (commit `7764ea0f912e53c92e82eb78a2a1631e92725fc8`, fetched from GitHub — neither the real `dinov2` repo nor a checkpoint is mounted in this sandbox, unlike the other sibling repos).
- [x] Corrected `load_cell_dino`'s construction path: the real path goes through `build_model_from_cfg`/`dinov2.eval.setup`, but that needs a full training-style cfg this pipeline doesn't have — **decided** to call the architecture factory (`vision_transformer.vit_large(...)`) directly instead, documented as a deliberate simplification in `docs/architecture.md`.
- [x] Confirm the checkpoint's state-dict key (`"teacher"` vs. top-level) against a real `.pth` — **unblocked**: `weights/cell_dino_vits8_pretrain_cp-37d20e9c.pth` is now present locally (gitignored, per the repo's blanket `*.pth` rule). Its state dict is **top-level, no wrapper key** (the "teacher" branch never fires for this file). Confirming this also surfaced a real architecture mismatch the original `in_chans=1`/`channel_adaptive=True` assumption got wrong for this specific checkpoint (`vit_small`/patch-8/**5-channel, not bag-of-channels**/chunked blocks/LayerScale) — see `docs/architecture.md`'s new "Real checkpoint" section for the full comparison table and the `load_cell_dino()`/`embed_batch()` fix (auto-detects `in_chans`/`img_size`/`block_chunks`/LayerScale from the checkpoint's own state dict instead of hardcoding the SPEC.md-assumed shape as the only supported one). `load_cell_dino()` now also raises (not just logs) on any non-empty `missing_keys` — the old silent-non-strict-load would have left 9 of this checkpoint's 12 transformer blocks at random initialization without a single warning above INFO level.
- [x] Confirmed the correct pooling operator: `DinoVisionTransformer.forward()` returns the CLS token straight through an identity head, so SPEC.md's mean/max-over-channel-CLS-tokens sketch is correct as written; documented why `channel_adaptive`'s own `get_intermediate_layers` bag-of-channels path (used by the paper's linear-probe scripts) is deliberately not used instead.
- [x] Recorded all of the above in `docs/architecture.md` (replacing the placeholder).

### Story 3.2 — Config & dataloader
- [x] `EmbedCellsConfig(AppConfig)` implemented per SPEC.md's dataclass (`shard_pattern`, `checkpoint_path`, `arch="vit_large"`, `patch_size=16`, `crop_size=224`, `channel_pool="mean"`, `mask_mode="none"`, `device="cuda"`, `batch_size=256`, `num_workers=4`). **Revised**: `mask_mode` replaced by `channels: list[int] = [0, 1, 2, 3]` (which of the crop's channel indices to embed, in order) and `channel_apply_mask: list[bool] = [True, True, True, True]` (same length as `channels`; per-channel whether the shared per-cell mask is applied) — see Story 3.3 and `docs/architecture.md`'s "Configurable input channels and per-channel masking" section.
- [x] `load_embedding_dataloader()` streams `(key, crop, mask, meta)` from `BUILD_DATASET`'s shards via `webdataset.WebDataset(...).decode().to_tuple(...).batched(...)`. **Correction versus SPEC.md's sketch**: verified against the installed `webdataset==1.0.2` that a bare glob `shard_pattern` (e.g. `"dataset-*.tar"`, what `embed_cells.nf` actually passes) is *not* expanded by `wds.WebDataset` itself -- only brace patterns are (`webdataset.shardlists.expand_urls`). `load_embedding_dataloader()` expands a non-brace pattern via `glob.glob()` itself before constructing the `WebDataset`, and raises a clear `ValueError` if that glob matches nothing.

### Story 3.3 — Model wrapper & masking
- [x] `load_cell_dino()` implemented against the real API confirmed in Story 3.1 (not the SPEC.md placeholder): builds via the named architecture factory and ports `dinov2.utils.utils.load_pretrained_weights`'s real checkpoint-key/prefix-stripping/non-strict-load logic. **Revised** once a real checkpoint surfaced the Story 3.1 mismatch above: no longer hardcodes `in_chans=1, channel_adaptive=True` — infers `in_chans`, `img_size` (decoupled from `cfg.crop_size`; only needs to match the checkpoint's `pos_embed` shape, since `interpolate_pos_encoding` reconciles any real-input-size difference at inference time), `block_chunks`, and whether LayerScale is present directly from the checkpoint's own state dict, and cross-checks `cfg.patch_size` against the checkpoint's own kernel size (clear `ValueError` on mismatch instead of a garbled grid). Escalates any non-empty `missing_keys` to a `RuntimeError` (`unexpected_keys` stays log-only, as before) — see `docs/architecture.md`.
- [x] `embed_batch()` implements bag-of-channels embedding: split `(B, C, H, W)` into `C` single-channel images, run through the shared backbone, pool per-channel CLS tokens (`channel_pool="mean"|"max"`) into `(B, D)`. **Generalized**: now branches on the loaded model's actual `patch_embed.in_chans` — `1` keeps this bag-of-channels path; any other value (a fixed-channel-count backbone, e.g. `cell_dino_vits8_pretrain`'s 5) is fed to the model jointly in one forward pass instead (no split, no `channel_pool`), raising a clear error if `cfg.channels` doesn't select exactly that many. Channel selection/reordering (`cfg.channels`) and per-channel masking (`cfg.channel_apply_mask`) both happen first, before this branch.
- [x] `mask_mode="zero_background"` zeroes non-target pixels using `mask.npy` before embedding; `mask_mode="none"` passes crops through untouched. Both paths covered by tests. **Superseded**: `mask_mode` (a single pipeline-wide toggle) replaced by `cfg.channel_apply_mask` (per-selected-channel) — every cell has exactly one shared `mask.npy` (SPEC.md §6.1), so this is "apply the shared mask to this channel or not," per channel, not per-channel-distinct masks. All-`False`/all-`True` reproduce the old `"none"`/`"zero_background"` behavior exactly.
- [x] Unit test: `embed_batch()` against a random-weight (not pretrained) model of the same architecture, confirming output shape `(B, D)`; the mask-zeroing and `mean`/`max` pooling behavior are each isolated and asserted against hand-computed expected values via a small deterministic stub backbone, independent of real transformer numerics. Plus new tests for the joint-multichannel `embed_batch()` path, `load_cell_dino()`'s chunked-block/LayerScale/in_chans/patch_size-mismatch/missing_keys inference, and `channels`/`channel_apply_mask` (selection/reorder, per-channel masking, length-mismatch and out-of-range validation), all against synthetic checkpoints/crops reproducing the real shapes.

### Story 3.4 — Output & Nextflow wiring
- [x] Output `embeddings.parquet`: one row per cell, `meta.json` fields passed through, plus zero-padded `emb_0000..emb_{D-1}` columns (`EMBEDDING_SELECTOR` from Epic 0 matches these).
- [x] `modules/local/embed_cells.nf` given `label 'process_gpu'` and wired to a GPU-capable executor/queue in `nextflow.config` -- confirmed already present in the scaffold, and its CLI arg names already match `EmbedCellsConfig`'s real field names exactly (unlike `qc_filter.nf`/`build_dataset.nf`, no Epic 9 reconciliation needed here). Gained a `channels=[...]`/`channel_apply_mask=[...]` list interpolation, mirroring `aggregate_embeddings.nf`'s `aggregators=[...]` precedent (Story 5.3); new `params.yaml` entries `cell_dino_channels: [0, 1, 2, 3]`/`cell_dino_channel_apply_mask: [true, true, true, true]`.
- [x] End-to-end smoke test (small synthetic shard, real `vit_small`-arch random-weight checkpoint via the CLI's `python -m fisseq_embeddings_pipeline.embed` entry point) produces `embeddings.parquet` with the expected shape -- the "(a) tiny random-init checkpoint" option SPEC.md §9.3 raises for CI, decided here since a real GPU/checkpoint isn't available in this environment anyway. **Additionally** re-run against both real checkpoints once they became available (CPU): `weights/cell_dino_vits8_pretrain_cp-37d20e9c.pth` (`arch=vit_small patch_size=8 crop_size=128`, 5-channel, `channels=[0,1,2,3,4]`) and `weights/channel_adaptive_dino_vitl16_pretrain_cells-ef7c17ff.pth` (this module's plain defaults, including the default `channels=[0,1,2,3]` selecting 4 of a larger synthetic crop) — both load with 0 missing/0 unexpected keys and produce finite, non-degenerate embeddings end to end via the CLI. See `tests/unit/test_embed.py::test_load_cell_dino_and_embed_batch_against_real_vits8_checkpoint`/`test_load_cell_dino_and_embed_batch_against_real_vitl16_checkpoint` (both skipped when their checkpoint file isn't present, e.g. in CI, since `weights/` is gitignored). `cell_dino_vitl16_...` matches SPEC.md's originally-documented production shape exactly (see `docs/architecture.md`); `EmbedCellsConfig`/`params.yaml`'s `checkpoint_path`/`cell_dino_checkpoint` remain deliberately required-with-no-default (SPEC.md §9.1) regardless, since a checkpoint path is inherently deployment-specific and `weights/` won't exist in every checkout.

---

## Epic 4 — Filter Embeddings: `FILTER_EMBEDDINGS` (SPEC.md §6.4)

*Goal: the no-copy/foreign-key redesign (SPEC.md §3 decision 10) — publish a join key + fitted stats, never a second copy of the embedding matrix.*

### Story 4.1 — `filter_and_fit_normalizer()`
- [x] Inner-joins `embeddings_lf` to `qc_passed_lf` on the composite key (`meta_batch`/`meta_well`/`meta_tile`/`meta_cell_index`).
- [x] Calls `variant_classification()` to mark `meta_is_control` (synonymous/untagged). **Placement note**: `variant_classification()` was ported into `filter.py` itself (not `utils/variant.py`, not `aggregate.py`) since Epic 4 needs it before Epic 5's `aggregate.py` exists — Epic 5/8 will import it from `.filter` rather than duplicating it, per `utils/variant.py`'s own Epic 0 correction note.
- [x] Fits `Normalizer.from_lazyframe(filtered, fit_only_on_control=True)`. `Normalizer` itself was vendored unchanged into a new `utils/normalizer.py` (not part of Epic 0 — it wasn't needed until now).
- [x] Returns `filtered_keys` (join key + `meta_is_control`/`meta_aa_changes`/other `meta_*` — **verified to contain zero `emb_*` columns**) and the fitted `Normalizer` — **never** a materialized normalized embedding table.
- [x] Unit test asserting `filtered_keys`'s column set contains no `emb_*` columns (this is the single most important regression test for decision 10 — write it before moving on). See `test_filtered_keys_has_no_embedding_columns` in `tests/unit/test_filter.py`.

### Story 4.2 — `load_filtered_embeddings()` (shared helper)
- [x] Joins `embeddings_lf` to `filtered_keys_lf` by composite key, then applies the normalizer. **Deviation from SPEC.md's sketch**: joins against only `JOIN_KEYS + [CONTROL_COLUMN_NAME]` from `filtered_keys_lf`, not the whole frame — `filtered_keys_lf` already carries every other `meta_*` column `embeddings_lf` has, verbatim, so joining the whole frame would collide and force Polars' `_right`-suffixing (caught by `test_load_filtered_embeddings_no_duplicate_columns`). See `filter.py`'s module docstring.
- [x] Importable from both `aggregate.py` (Epic 5) and `ovwt.py` (Epic 6) without duplicating the join/apply logic — `load_filtered_embeddings`/`variant_classification` are public functions in `filter.py`; full exercise of this happens once Epic 5/6 actually import them.
- [x] Unit test: `load_filtered_embeddings()` output matches what the *old* single-step `filter_and_normalize()` (SPEC.md's superseded version) would have produced, on the same synthetic input — i.e. the redesign is output-equivalent, not just differently-shaped. See `test_load_filtered_embeddings_output_equivalent_to_old_single_step_approach`.

### Story 4.3 — Output & Nextflow wiring
- [x] Outputs `filtered_keys.parquet`, `normalizer.parquet` (no `filtered_embeddings.parquet`). Hydra `main()` entry point implemented in `filter.py`, exercised end-to-end via `test_main_runs_end_to_end_via_cli` (subprocess, mirroring `test_qcfilter.py`'s pattern).
- [x] `modules/local/filter_embeddings.nf` takes `embeddings.parquet` + `filtered_cells.parquet` as input, matching the stub's `input:` block. Added the previously-missing `label_column=${params.filter_label_column}` CLI arg (every other field was already wired) and a new `filter_label_column` entry in `params.yaml`, matching `embed_cells.nf`'s convention of wiring every config field explicitly, even ones with defaults.
- [ ] `workflows/embeddings.nf`'s `embed_and_filtered_ch = embed_ch.join(filtered_ch)` wiring (feeding both Epic 5 and Epic 6) is in place and tested against a real (small) Nextflow run once Epic 9 starts.

---

## Epic 5 — Aggregation: `AGGREGATE_EMBEDDINGS` (SPEC.md §6.5)

*Goal: per-variant pooling of the (reconstructed) synonymous-corrected embeddings — generalized beyond SPEC.md's original median-only sketch to support any combination of mean/median/KS/AUROC (user-directed scope expansion, mirroring fisseq-data-pipeline's `BaseAggregator`/`ReferenceBasedAggregator` architecture), with a backward-compat rule keeping the single-method default byte-shape-identical to SPEC.md's original Output note.*

**Deviation from SPEC.md's original sketch, applying to every story below**: all aggregators, including mean/median, exclude control (synonymous, untagged) rows before grouping by variant — matching fisseq-data-pipeline's `BaseAggregator._native_aggregate_feature_batch` (`lf.filter(~CONTROL_COLUMN).group_by(label_col)`) exactly, not SPEC.md's original literal every-row `group_by()`. Required structurally for KS/AUROC (comparing the reference pool to itself is meaningless); applied uniformly to mean/median too for one consistent rule instead of a per-method special case — user-confirmed decision. Literal `"WT"` rows are unaffected (`classify_variant("WT") == "WT"`, never `"Synonymous"`, so WT is never marked control) — only genuinely-synonymous variant labels drop out of the per-variant output.

### Story 5.1 — Aggregator classes & registry
- [x] `BaseAggregator`/`ReferenceBasedAggregator` ported from `fisseq_data_pipeline.aggregate` (trimmed: no `per_barcode`/`block_list`/`barcode_column`, no WT-null-bootstrap `null_statistic_transform`/`null_comparison_statistic` — SPEC.md §6.5's Resolved notes already ruled out `per_barcode` and WT-null/blocklist for v1). `_feature_columns` keys off `EMBEDDING_SELECTOR`, not `FEATURE_SELECTOR`.
- [x] `MeanAggregator`, `MedianAggregator`, `KSAggregator`, `AUROCAggregator` ported (only `MAD`/`std`/`signedKS`/`QQ`/`*negLogP` are out of scope — not requested; add via the same registry pattern later if ever needed).
- [x] `_AGGREGATORS: dict[str, type[BaseAggregator]]` registry with exactly `{"mean", "median", "KS", "AUROC"}`.
- [x] Unit tests: KS matches `scipy.stats.ks_2samp`, AUROC matches `sklearn.metrics.roc_auc_score` (unsymmetrized) across RANDOM/TIES/SINGLE value shapes; mean/median match `np.mean`/`np.median`; all four aggregators exclude control rows uniformly; a literal `"WT"` label (not classified control) still gets its own row.

### Story 5.2 — `aggregate_embeddings()` combination & backward-compat
- [x] Takes `filtered_lf` (built by the **caller** via `load_filtered_embeddings()`, Epic 4 — not read from a file directly) plus a `label_column` and an `aggregators: Sequence[str]` (defaults to `("median",)`).
- [x] Validates `aggregators` up front (empty, unknown name, or duplicate name all raise `ValueError` before any aggregator runs — mirrors `fisseq_data_pipeline.aggregate.aggregate()`'s fail-fast validation).
- [x] Runs each requested aggregator and joins their outputs on `label_column` (each aggregator's own `_stat_suffix` already namespaces columns, so no collision across methods).
- [x] Output-shape backward-compat rule: when `aggregators == ("median",)` (the default), strips the `_median` suffix so output columns are bare `emb_0000..emb_{D-1}`, matching SPEC.md's original Output note exactly; any other selection keeps suffixed columns (`emb_0000_mean`, `emb_0000_KS`, etc.).
- [x] Joins in `get_aggregate_meta_data()` (vendored unchanged, new `utils/metadata.py`) for `meta_num_cells`/`meta_barcode_num_unique`/etc. — the control/WT-label metadata row is naturally dropped by the join (no separate filtering needed, since no aggregator ever produces a control-row group to join against).
- [x] Confirmed **no** WT-null bootstrap/blocklist reproducibility-gate machinery is ported (SPEC.md §6.5's second Resolved note — explicitly out of scope for v1).
- [x] Unit tests: default single-method output is unsuffixed; multi-method output is suffixed with no collisions; a single non-median method is also suffixed; empty/unknown/duplicate `aggregators` all raise.
- [x] SPEC.md §6.5's code sketch and Output note updated to reflect the generalized design and the control-row-exclusion deviation above.

### Story 5.3 — Output & Nextflow wiring
- [x] `AggregateEmbeddingsConfig(AppConfig)`: `embeddings_file`/`filtered_keys_file`/`normalizer_file` (`str = MISSING`, matching `aggregate_embeddings.nf`'s already-fixed CLI contract), `label_column: str = "meta_aa_changes"`, `aggregators: List[str]` (default `["median"]`). Hydra `main()` entry point reads all three parquet files, reconstructs `filtered_lf` via `load_filtered_embeddings()`, calls `aggregate_embeddings()`, writes `{prefix}aggregate.parquet` (no `filtered_embeddings.parquet` or other materialized copy).
- [x] `modules/local/aggregate_embeddings.nf` gains the previously-missing `label_column=${params.filter_label_column}` (direct precedent: `filter_embeddings.nf`) and a new `aggregators=[...]` CLI arg, single-quoted (`'aggregators=[${params.aggregate_methods.join(",")}]'`, matching `embed_cells.nf`'s `'shard_pattern=./*.tar'` precedent for shell-glob-unsafe characters) sourced from a new `params.yaml` entry `aggregate_methods: ["median"]`. **New pattern with no prior precedent in this repo** (no `List`-typed `params.yaml` entry or Groovy list-to-CLI interpolation existed before this story) — the Groovy-interpolation half is unverified until Epic 9's real Nextflow run; the Python/Hydra half (`AggregateEmbeddingsConfig.aggregators` parsing a bracketed CLI override) is covered by a direct subprocess CLI test in this story.
- [x] Unit test: CLI end-to-end (subprocess, mirroring `test_qcfilter.py`'s/`test_filter.py`'s pattern) — once with default config (unsuffixed `emb_*` columns) and once with `'aggregators=[mean,median]'` (both suffix families present). See `test_main_runs_end_to_end_via_cli_default`/`_multi_method` in `tests/unit/test_aggregate.py`.
- [ ] `workflows/embeddings.nf`'s `agg_ch = AGGREGATE_EMBEDDINGS(embed_and_filtered_ch)` wiring is in place and tested against a real (small) Nextflow run once Epic 9 starts.

---

## Epic 6 — OVWT Distinguish-ability Scores: `OVWT_BATCHWISE` (SPEC.md §6.6)

*Goal: k-fold CV stratified jointly on `(meta_barcode, is_wt)`, with per-fold calibration, producing an out-of-fold score for every cell and two distinguish-ability numbers per variant. The most algorithmically involved epic — budget real review time for the fold/stratification logic.*

### Story 6.1 — Config
- [x] `OvwtEmbeddingConfig(AppConfig)` with `label_column`, `wt_label="WT"`, `n_folds: int = 5`, `calibrate: bool = True`, `min_cells=250`, `downsample_wt=True` (thresholds carried over from `ovwt.py` as a starting point, per SPEC.md's Resolved note), plus the vendored `xgboost: XGBoostConfig` sub-config. **Scope note**: `downsample_wt` is a plain `bool` here, not fisseq-data-pipeline's `Union[bool, int]` explicit-target variant — not requested by this story's field list.
- [x] No `random_state` field — inherits `random_seed` from `AppConfig` only (Epic 0).

### Story 6.2 — `predict_binary()` helper
- [x] Thin wrapper around vendored `get_dmatrix()` + `model.predict()`, no metric computation.
- [x] Unit test against a trivially-separable synthetic dataset (predicted scores near 0/1 in the expected direction). Note: with `early_stopping_rounds=5` scores don't fully saturate to 0/1 even on trivially-separable data, so the test asserts clear directional separation (`>0.7`/`<0.3`) rather than a stricter `>0.9`/`<0.1` bound.

### Story 6.3 — `ovwt_batchwise()` core loop
- [x] Per non-WT variant: builds the variant-vs-WT `subset`, computes the composite `(meta_barcode, is_wt)` stratification key. **Rare-stratum fallback baked in from the start** (see Story 6.4): any stratum below `_MIN_STRATUM_SIZE = 10` members collapses into a shared `"rare|wt"`/`"rare|variant"` bucket, since the core loop isn't meaningfully testable without it.
- [x] `sklearn.model_selection.StratifiedKFold(n_splits=cfg.n_folds, shuffle=True, random_state=cfg.random_seed)` splits the subset.
- [x] Per fold: inner fit/calibration split via `split_indices_stratified` (seeded `cfg.random_seed + fold_idx`); `train_binary_xgboost(train_df, calib_df, ..., xgb_cfg)` — **passing `xgb_cfg = OmegaConf.structured(cfg)`** (computed once before the loop, reused for every fold/variant), not `cfg` or `cfg.xgboost` directly: `train_binary_xgboost` internally does `dict(cfg.xgboost.params)`, which raises `TypeError` on a plain dataclass `cfg` — confirmed empirically during planning. This is the fixed call site SPEC.md §6.6's code comment flags (needs both `.xgboost.*` and `.random_seed` off the same object).
- [x] If `cfg.calibrate`: fits a sigmoid/Platt calibrator (`sklearn.calibration._SigmoidCalibration`) on the calibration slice's raw scores; stores `(model, calibrator)` per fold.
- [x] Every cell in `subset` receives exactly one out-of-fold score (`oof_scores[test_idx] = ...`) — unit test asserts no `NaN`s remain in `oof_scores` after all folds run (`test_ovwt_batchwise_no_nans_in_oof_scores`).
- [x] `auroc_pooled` computed over the full subset's OOF scores.
- [x] `auroc_median_barcode`: per non-WT barcode, AUROC of that barcode's cells vs. all WT cells (same OOF scores), then median across barcodes — **WT barcodes excluded** from this median (verified with `test_ovwt_batchwise_auroc_median_barcode_excludes_wt`, >1 barcode per variant with deliberately different separability). **`None`, not `float("nan")`**, when the barcode list is empty (a defensive-only branch in practice) — `float("nan")` would silently poison Polars' `.mean()`/skew its `.median()` in Epic 8's later cross-experiment pooling step, confirmed empirically; `None` is excluded cleanly, matching `Normalizer.apply()`'s established null-over-NaN convention.
- [x] Output columns exactly `<label_column>` (not a hardcoded `"meta_aa_changes"` literal — uses `cfg.label_column`, consistent with Epic 5's `aggregate_embeddings()`), `auroc_pooled`, `auroc_median_barcode`, `meta_n_barcodes`, `meta_n_cells` (note the `meta_` prefix on the count columns — required for Epic 8's z-score step to exclude them automatically via `FEATURE_SELECTOR`).
- [x] `cell_scores.parquet` populated directly from the OOF loop: `meta_*` + `score` + `meta_variant_scored_against`, one row per cell per variant it was scored against.
- [x] `models.pkl`: `dict[str, list[tuple[Booster, calibrator | None]]]`.
- [x] Per-variant resilience: a variant whose fold training/evaluation raises is skipped with a logged warning rather than aborting the run, ported from fisseq-data-pipeline's `ovwt.py::main()`'s own per-variant `try`/`except` pattern — the actual backstop for strata too small for the inner split to survive, not the `_MIN_STRATUM_SIZE` threshold alone (see Story 6.4). Code path implemented here; dedicated test in Story 6.4.
- [x] Empty-result edge case (found during planning, not in SPEC.md's sketch): if every variant is filtered out or every variant's loop raises, `pl.concat([])` on an empty list would raise — handled by returning correctly-schema'd empty `pl.DataFrame`s instead. Code path implemented here; dedicated test in Story 6.4.

### Story 6.4 — Stratification edge case (SPEC.md §6.6's Resolved note)
- [x] Decide and implement a concrete fallback for barcodes with fewer than `n_folds` cells (StratifiedKFold will otherwise raise). **Chosen approach, a two-layer mitigation, not a single threshold**: (1) `_stratification_key()` collapses any `(barcode, is_wt)` stratum below `_MIN_STRATUM_SIZE = 10` into a shared `"rare|wt"`/`"rare|variant"` bucket — this alone only guarantees the *outer* `StratifiedKFold.split()` won't raise; (2) the per-variant `try`/`except` in `ovwt_batchwise()` is the actual backstop against the *inner* `split_indices_stratified` nested split failing on a bucket that's still too small (confirmed empirically during planning: that inner 80/10/10 split needs ~8-13 members per class, not just `>= n_folds`, and that floor is too data-dependent to hard-code safely). Covered by `test_ovwt_batchwise_rare_barcode_does_not_crash_and_is_skipped` (a 2-cell barcode is caught and skipped without aborting the run; a normal variant in the same run still succeeds) and `test_ovwt_batchwise_all_variants_filtered_out_returns_empty_frames` (the degenerate all-filtered-out case).
- [ ] Confirm real per-variant/per-barcode cell counts from an actual experiment before finalizing `min_cells`/`n_folds` defaults; adjust `params.yaml` if the defaults turn out wrong at scale. **Not done** — no real experiment data is available in this environment (consistent with Epic 1 Story 1.3's same limitation); flagged for re-verification once real data exists.

### Story 6.5 — Integration with Epic 4
- [x] Takes `embeddings.parquet` + `filtered_keys.parquet` + `normalizer.parquet` as three inputs; constructs `filtered_lf` via Epic 4's `load_filtered_embeddings()` before calling `ovwt_batchwise()`. Hydra `main()` entry point implemented in `ovwt.py`, mirroring `aggregate.py`'s Story 5.3 pattern exactly: reads the three parquet files, reconstructs `filtered_lf`, calls `ovwt_batchwise()`, writes `{prefix}results.parquet`/`{prefix}cell_scores.parquet`/`{prefix}models.pkl` to `output_dir`.
- [x] `modules/local/ovwt_batchwise.nf`'s script block already matched `OvwtEmbeddingConfig`'s real field names (`embeddings_file`/`filtered_keys_file`/`normalizer_file`/`n_folds`/`calibrate`/`min_cells`/`downsample_wt`/`random_seed`) — no Epic 9 CLI-arg reconciliation needed here, unlike `qc_filter.nf`/`build_dataset.nf`. Comment updated to point at Epic 9's remaining `workflows/embeddings.nf` wiring instead of a bare Epic 9 TODO.
- [x] Unit test: CLI end-to-end (subprocess, mirroring `test_aggregate.py`'s pattern) — builds `embeddings.parquet`/`filtered_keys.parquet`/`normalizer.parquet` via `filter_and_fit_normalizer()` the way `FILTER_EMBEDDINGS` would, runs `python -m fisseq_embeddings_pipeline.ovwt` via subprocess, and confirms `results.parquet`/`cell_scores.parquet`/`models.pkl` all land with the expected shape. See `test_main_runs_end_to_end_via_cli` in `tests/unit/test_ovwt.py`.
- [ ] `workflows/embeddings.nf`'s wiring feeding `OVWT_BATCHWISE` from the same `embed_and_filtered_ch` as `AGGREGATE_EMBEDDINGS` is in place and tested against a real (small) Nextflow run once Epic 9 starts (same deferral as Epic 4 Story 4.3's and Epic 5 Story 5.3's own last bullet).

---

## Epic 7 — Global Variant Embeddings: `GLOBAL_VARIANT_EMBEDDINGS` (SPEC.md §6.7)

*Goal: cross-experiment median pooling, then PCA. Runs once, unconditionally, over every experiment (SPEC.md §3 decision 8 — no `global_channels`-style scoping).*

### Story 7.1 — `global_variant_embeddings()`
- [x] `median_across_batches()` vendored unchanged from `globalfeatureselect.py` (real source read from the sibling `fisseq-data-pipeline` checkout), into a new `utils/globalfeatureselect.py` -- only this one function, since the rest of that source file (blocklist combination, per-feature-type output, pycytominer feature selection, impact-score) is out of scope for SPEC.md §6.7's own (much smaller) sketch.
- [x] `compute_pca()` called with `random_state=cfg.random_seed` (Epic 0's added parameter).
- [x] **Revised (per request): no `n_components` config field at all.** `GlobalVariantEmbeddingsConfig` drops the field entirely; `global_variant_embeddings()` always computes PCA at the full retained rank (`min(n_variants, n_retained_feature_dims)`, mirroring -- not importing -- `compute_pca`'s own private all-null-feature-column drop) rather than SPEC.md's original fixed default of 50. See SPEC.md §6.7's Revision note.
- [x] **Revised (per request): four output files, not three.** `compute_pca`'s combined `components_df` (loadings + both variance-explained columns in one frame) is split at the `global_embeddings.py` call site into `pca_components.parquet` (loadings only) and a new `pca_variance_explained.parquet` (`meta_component_idx`, `meta_variance_explained`, `meta_cumulative_variance_explained`) -- `compute_pca` itself is untouched. Output: `median_aggregate.parquet`, `pca_scores.parquet` (`meta_aa_changes`, `meta_pc_1..meta_pc_{n}`), `pca_components.parquet`, `pca_variance_explained.parquet`.
- [x] `modules/local/global_variant_embeddings.nf` wired: `path(aggregate_parquets, stageAs: "agg_input_*.parquet")` avoids every experiment's identically-named `aggregate.parquet` colliding when collected into this one task (`fisseq-data-pipeline`'s own `globalfeatureselect.py` precedent for the same collision) -- `GlobalVariantEmbeddingsConfig.batch_stems: List[str]` is reconstructed against `agg_input_1.parquet..agg_input_{n}.parquet` positionally (new shared `utils/nextflow_staging.py::reconstruct_staged_paths`, since Epic 8 needs the identical logic). Unverified Groovy-interpolation half until Epic 9's real Nextflow run, same caveat as `embed_cells.nf`'s/`aggregate_embeddings.nf`'s own list-interpolation precedents. New `params.yaml` has no `global_variant_embeddings_n_components` entry (see the Revision note above).
- [x] Unit tests: PCA over a small synthetic multi-experiment aggregate table (`tests/unit/test_global_embeddings.py`) confirming the full-retained-rank component count (not a fixed 50, which the tiny synthetic table couldn't even support), the four-file split (no variance columns in `pca_components.parquet`, only variance columns in `pca_variance_explained.parquet`, monotonically non-decreasing cumulative variance ending at ~1.0), `random_seed` reproducibility, and a CLI end-to-end subprocess test exercising the real `agg_input_N.parquet` staged-file convention. `utils/globalfeatureselect.py`'s `median_across_batches()` also covered directly (`tests/unit/utils/test_globalfeatureselect.py`) -- empty-input/no-common-column errors, cross-batch median collapsing, single-batch passthrough, dropped-column pruning.
- [x] **Further revision (per request): a fifth output, `pca_reduced.parquet`.** `GlobalVariantEmbeddingsConfig.cumulative_variance_explained: float = 0.9` (validated to `(0, 1]`, raising `ValueError` otherwise) selects the smallest leading-component prefix reaching that cumulative variance (`_n_components_for_variance`, falling back to full rank if never reached) -- this affects only `pca_reduced.parquet`, not the three always-full-rank PCA files above. `pca_reduced.parquet` = that truncated `meta_pc_*` matrix plus `meta_is_control` (re-derived via `filter.py`'s `variant_classification`, "propagating" the control/synonymous metadata `median_across_batches` otherwise collapses away -- mirrors `globalfeatureselect.py`'s own real `main()` step 6) plus `meta_impact_score` (new `utils/vectors.py`, vendoring only `compute_impact_score`/`compute_cosine_distance` from `fisseq-data-pipeline`'s `utils/vectors.py`), **computed on the reduced PC matrix itself** per request (not pre-PCA on the raw features, unlike the real repo's own ordering) -- `compute_impact_score`'s `FEATURE_SELECTOR`-based column detection would otherwise treat the `meta_pc_*` columns as metadata, not features, so `global_embeddings.py` temporarily strips their `meta_` prefix for that one call and restores it after. `modules/local/global_variant_embeddings.nf`/`params.yaml` gain `cumulative_variance_explained`/`global_variant_embeddings_cumulative_variance_explained` (default `0.9`), wired the same way as every other explicitly-wired field. Unit tests: `_n_components_for_variance`'s threshold-selection logic directly (exact match, never-reached fallback), the reduced matrix is a true truncation of the full one (byte-identical leading columns, not a re-fit), `meta_is_control`/`meta_impact_score` correctness against a synthetic control population, `cumulative_variance_explained` validation, and a CLI end-to-end check that the param is actually configurable. `utils/vectors.py`'s `compute_impact_score` also covered directly (`tests/unit/utils/test_vectors.py`) -- control/opposite/orthogonal-direction score sanity checks, intermediate-column cleanup.

---

## Epic 8 — Global Variant Distinguish-ability Scores: `GLOBAL_VARIANT_DISTINGUISHABILITY` (SPEC.md §6.8)

*Goal: two-step pooling (SPEC.md §3 decision 9) — per-experiment synonymous z-score, then cross-experiment median. Not a direct median of raw AUROC.*

### Story 8.1 — `global_variant_distinguishability()`
- [x] Per experiment: `variant_classification()` + `Normalizer.from_lazyframe(fit_only_on_control=True)` fit **fresh per experiment** on that experiment's own `results.parquet` (not reused from Epic 4/6's per-cell normalizer — different population, different unit). Imported from `filter.py`/`utils/normalizer.py` rather than duplicated, per `filter.py`'s own module docstring.
- [x] `Normalizer.apply()` called unmodified — confirmed it only touches `auroc_pooled`/`auroc_median_barcode` and leaves `meta_n_barcodes`/`meta_n_cells` alone, since Epic 6 already named those columns `meta_*` (SPEC.md §6.6's Output note).
- [x] Cross-experiment median of the **z-scored** values (not raw AUROC) into `meta_median_auroc_pooled`/`meta_median_auroc_median_barcode`, plus `meta_num_experiments`. **Correction versus SPEC.md's sketch**: `variant_classification` takes a `pl.LazyFrame`, not the raw `pl.DataFrame` the sketch passed it -- fixed to `df.lazy()` (see SPEC.md §6.8's Correction note).
- [x] `modules/local/global_variant_distinguishability.nf` wired with the same `stageAs: "res_input_*.parquet"` collision-avoidance pattern as Epic 7 (every experiment's `results.parquet` shares its filename too) -- `GlobalVariantDistinguishabilityConfig.batch_stems: List[str]` reconstructs `res_input_1.parquet..res_input_{n}.parquet` via the same shared `utils/nextflow_staging.py` helper, used here only for staged-filename reconstruction/logging (the core function itself needs no per-batch label).
- [x] Unit test covering the graceful-degradation path: an experiment with a single (or zero-variance) synonymous population should null out that column rather than raising, and polars' `.median()` should silently exclude the null from the pooled result (SPEC.md's Resolved note) — `test_global_variant_distinguishability_graceful_degradation_zero_variance`.
- [x] Unit test: synonymous variants' own z-scored values land near 0 (the built-in sanity check SPEC.md calls out) — `test_global_variant_distinguishability_synonymous_lands_near_zero`. Plus a dedicated test that a shared variant scored against two experiments with different synonymous baselines lands where a *z-scored* pool would put it, not a naive median of raw AUROC (`test_global_variant_distinguishability_medians_zscored_not_raw_auroc`), and a CLI end-to-end subprocess test.

---

## Epic 9 — Nextflow orchestration (SPEC.md §7)

*Goal: wire every module above into a working end-to-end `nextflow run`.*

### Story 9.1 — `main.nf` / `workflows/embeddings.nf`
- [x] `configsDir` validation (`pipeline_dir` required, at least one `configs/*.yaml`) implemented with clear `error` messages, including an explicit `isDirectory()` check (the scaffold's `?.listFiles()` alone would silently yield `[]` — and thus the generic "no .yaml files found" message — for a typo'd path that isn't a directory at all; `fisseq-data-pipeline`'s own `workflows/fisseq.nf` distinguishes these two cases, now matched here).
- [x] `config_ch` construction from `config_files`: each `configs/<batch_stem>.yaml` is parsed via `org.yaml.snakeyaml.Yaml()` (Groovy, same library `fisseq-data-pipeline`'s `workflows/fisseq.nf` uses) into a `Map`, `batch_stem` taken from the filename (a `batch_stem` key inside the file, if present, is dropped -- the filename is always authoritative). Passed to `BUILD_DATASET` as `tuple(batch_stem, batch_config)`, not the raw file, so `-resume`'s cache key is the actual scalar values (a non-semantic YAML edit shouldn't bust the cache) -- same rationale as `fisseq-data-pipeline`'s `INPUT`/`BatchParams.resolve` precedent for this identical problem.
- [x] Full per-batch chain wired: `BUILD_DATASET -> {QC_FILTER, EMBED_CELLS} -> FILTER_EMBEDDINGS -> {AGGREGATE_EMBEDDINGS, OVWT_BATCHWISE}` per the scaffold's sketch, with real (not glob-string) path channels feeding the two global processes. **Bug fixed along the way**: `embed_ch.join(qc_ch)` was joining in all 4 of `QC_FILTER`'s output fields (`filtered_cells`, `barcode_counts`, `variants_per_barcode` alongside `batch_stem`), but `FILTER_EMBEDDINGS` only declares 2 input path slots (`embeddings_parquet`, `filtered_cells_parquet`) -- would have failed at the first real run. Fixed to `qc_ch.map { ... -> tuple(s, filtered_cells) }` before the join. `include { ... }` statements for all 8 processes were also missing from the scaffold's `workflows/embeddings.nf` entirely (it referenced process names with no imports) -- added.
- [x] `-resume` verified to actually skip unchanged per-batch stages on a second run — `java`/`nextflow` were installed into this sandbox (`openjdk-17-jre-headless` + the official `get.nextflow.io` installer, now also recorded in `.devcontainer/devcontainer.json`'s `features`/`postCreateCommand`) and a real synthetic-experiment run was executed twice: first run `[SUCCESS] completed=8 failed=0 cached=0`, second run with `-resume` and identical inputs `[SUCCESS] completed=0 failed=0 cached=8` — every one of the 8 processes was skipped from cache. `docker` is still not installed here, so this was run under the new `local` profile (`nextflow.config`), not the containerized default path.

### Story 9.2 — Remaining `modules/local/*.nf`
- [x] `build_dataset.nf` rewritten: the scaffold's `--config-path=. --config-name=${config_yaml}` doesn't actually work against this repo's Hydra setup (`dataset.py`'s `hydra.main` is registered with a fixed `config_name="dataset_main"`/`config_path=None` via `ConfigStore` -- there is no config-file-loading mode to hand it an arbitrary external YAML the way that flag pair implies, and the fixed `config_name` wouldn't match an arbitrary per-experiment filename regardless). Replaced with per-key CLI overrides built from `workflows/embeddings.nf`'s parsed `batch_config` map (same `List` -> `'key=[a,b]'` interpolation convention as `aggregate_embeddings.nf`/`embed_cells.nf`).
- [x] `qc_filter.nf` **field-name bug fixed**: it was passing `barcode_count_threshold=`/`variant_barcode_count_threshold=` (params.yaml's key names), but `QcFilterConfig`'s actual fields are `bc_threshold`/`variant_bc_threshold` -- Hydra would have silently ignored both overrides and always run with the hardcoded defaults (10 / 4) no matter what `params.yaml` said. `cell_files`/`edit_distance_threshold` were already correct.
- [x] `filter_embeddings.nf`, `aggregate_embeddings.nf`, `ovwt_batchwise.nf` cross-checked field-by-field against `FilterEmbeddingsConfig`/`AggregateEmbeddingsConfig`/`OvwtEmbeddingConfig` -- all already correct, no changes needed.
- [x] `global_variant_embeddings.nf`, `global_variant_distinguishability.nf` cross-checked against `GlobalVariantEmbeddingsConfig`/`GlobalVariantDistinguishabilityConfig` (Epics 7-8) -- already correct.
- [x] Every module: `errorStrategy 'ignore'`, `container "${params.container_image}"`, `publishDir`, trailing `random_seed=${params.random_seed}` — confirmed present on all 8.

### Story 9.3 — End-to-end smoke run
- [x] A full `nextflow run . --pipeline_dir <tiny-synthetic-experiment> -params-file params.yaml` completes with exit code 0 against the real (not stubbed) modules — run for real (via `tests/integration/test_integration.py`, `-profile local`, `nextflow`/`java` now installed in this sandbox) and green: all 5 integration tests pass, `[SUCCESS] completed=8 failed=0 cached=0`. `docker` is still not installed here, so the containerized default path (no `-profile` flag) itself remains unexercised -- only `-profile local`'s no-container path has been run.
- [x] Output directory tree matches SPEC.md §8 exactly (spot-check every file listed there exists) — asserted and passing in `test_integration.py` (`test_dataset_and_embeddings_produced`, `test_filter_embeddings_has_no_embedding_columns`, `test_aggregate_and_ovwt_outputs_exist`, `test_global_stage_outputs_exist`).

**Real bug this run surfaced and fixed**: `utils/nextflow_staging.py`'s `reconstruct_staged_paths` assumed Nextflow always 1-indexes a `stageAs: "<prefix>_*.parquet"` collection (`agg_input_1.parquet`, ...) -- true for 2+ staged files, confirmed against a real `nextflow run`, but for exactly **one** staged file (i.e. a single-experiment pipeline run) Nextflow instead substitutes `*` with an empty string, staging it as `agg_input_.parquet` with no digit at all. `GLOBAL_VARIANT_EMBEDDINGS`/`GLOBAL_VARIANT_DISTINGUISHABILITY` both failed with `FileNotFoundError: agg_input_1.parquet` against this fixture's one batch until fixed. New regression tests in `tests/unit/utils/test_nextflow_staging.py`; `tests/unit/test_global_embeddings.py`'s own single-batch CLI fixtures were also using the wrong (now-corrected) staged filename and needed updating to match.

---

## Epic 10 — Configuration & containerization (SPEC.md §9.1, §9.2)

### Story 10.1 — `params.yaml` completeness & fail-fast
- [x] Every Hydra config field introduced in Epics 1-8 has a corresponding `params.yaml` entry (cross-check against the scaffold's starter file — it may be incomplete). **Audit findings:** every `params.*` reference in `modules/local/*.nf` already had a `params.yaml` entry except two real gaps, now fixed: `ovwt_batchwise.nf` never passed `label_column`/`wt_label` at all (silently kept `OvwtEmbeddingConfig`'s own Hydra defaults regardless of what `filter_label_column` was overridden to — every other label-column-consuming stage already keyed off that one shared param); fixed by wiring `label_column=${params.filter_label_column}` and a new `ovwt_wt_label` param. `qc_filter.nf` gained the same `label_column=${params.filter_label_column}` wiring for consistency (was previously left at `QcFilterConfig`'s own default, which happened to already match). **Deliberately left unwired**, documented in `params.yaml`/`qc_filter.nf`'s comments rather than silently absent: `BuildDatasetConfig`'s fields (per-experiment, sourced from `configs/<batch_stem>.yaml`, not `params.yaml`, by design — a single global value could never be right across every experiment's own wells/grid/raw-CSV-column layout); `QcFilterConfig`'s `barcode_col_name`/`aa_changes_col_name`/`edit_distance_col_name` (structural — name `BUILD_DATASET`'s fixed `meta_*` output columns, never meant to vary per run); `QcFilterConfig`'s opt-in `n_variants`/`variant_downsample_classes`/`variant_downsample_mode`/`variant_allow_list_file` cap (off by default; `variant_downsample_classes`' default value contains a space, which this repo's `List` → `'key=[a,b]'` Groovy-interpolation convention can't round-trip safely without per-element quoting this story didn't need to solve for a disabled-by-default feature).
- [x] Required-with-no-default params (`pipeline_dir`, `cell_dino_checkpoint`) fail with a clear, specific error message when unset — **not** Nextflow's generic "no such property" (SPEC.md §9.1's Resolved note) — write a test exercising this. `pipeline_dir` was already checked; **`cell_dino_checkpoint` had no check at all** (a real gap this story fixes in `workflows/embeddings.nf`) — Nextflow's script block would have failed deep inside `EMBED_CELLS` with `checkpoint_path=null` instead of failing at workflow-definition time with a clear message. Both checks verified against a real `nextflow run` (this sandbox now has `nextflow`/`java` installed, per Epic 9's precedent): exit code 1, specific message, no process scheduled. `tests/integration/test_integration.py::test_fails_fast_when_pipeline_dir_missing`/`test_fails_fast_when_cell_dino_checkpoint_missing` (subprocess, real `nextflow run`, no synthetic fixture needed since the failure happens before any process is scheduled).

### Story 10.2 — Docker image
- [x] `Dockerfile` builds successfully (`docker build -t fisseq-embeddings-pipeline:latest .`). **Unblocked and done**: the devcontainer gained `docker-outside-of-docker` (mounts the host's real Docker Desktop socket — see the two Epic 10 follow-up commits), and a real `docker build` now succeeds end to end (~3.5 min, ~4.6GB image). Before this, the exact `COPY`/`RUN` layer ordering was reproduced by hand against a scratch directory with `uv` and caught two real bugs (see the earlier Epic 10.2 commit): (1) `uv sync --frozen` in the dependencies-only layer needs `--no-install-project` (no `src/`/`README.md` yet); (2) nothing put the `uv`-managed `.venv/bin` on `PATH`. **A real build then caught a third, more serious bug those hand-simulations couldn't**: the Dockerfile only copied `pyproject.toml`/`uv.lock`, never `.python-version` (which pins `3.13`) — inside the actual image build, with no `.python-version` present, `uv sync` resolved `pyproject.toml`'s open-ended `requires-python = ">=3.13"` to the newest available interpreter (**3.14.7**), and Hydra 1.3.x's `get_args_parser()` crashes outright on Python 3.14's stricter `argparse._check_help` (`TypeError: argument of type 'LazyCompletionHelp' is not a container or iterable`) — breaking **every single stage's CLI entry point**, not just `--help`. Fixed by copying `.python-version` in before either `uv sync` call, and (belt-and-suspenders) tightening `pyproject.toml`'s `requires-python` to `>=3.13,<3.14` so the same floor-only mistake can't recur even somewhere `.python-version` isn't copied/respected — `uv lock` regenerated accordingly (a pure narrowing: drops now-irrelevant 3.14/3.15 wheel entries, no dependency version changes). Rebuilt after the fix: `python --version` inside the image is `3.13.15`, confirmed.
- [x] Image actually runs each stage's `python -m fisseq_embeddings_pipeline.<module>` command successfully, GPU stage included (verify `--gpus all` / `nvidia-smi` inside the container on a GPU host). **Partially done, real verification**: `python -m fisseq_embeddings_pipeline.<module> --help` was run inside the actual built image (`docker run`) for all 8 stages (`dataset`/`qcfilter`/`embed`/`filter`/`aggregate`/`ovwt`/`global_embeddings`/`global_distinguishability`) — all exit 0, confirming `PATH`/module resolution/Hydra parsing all work for real inside the container, not just in the scratch-directory simulation. Also confirmed `import fisseq_embeddings_pipeline.vendor.dinov2...` works inside the image (dinov2 vendoring, see below). **Still not done**: a full `nextflow run` using the actual containerized executor (`docker.enabled=true`, the production default — not `-profile local`) against this devcontainer's real Docker daemon hit a structural docker-outside-of-docker limitation, not a pipeline bug — `docker inspect` on this devcontainer confirms `/workspaces/fisseq-embeddings-pipeline` is bind-mounted from `/Users/gwen/Documents/fisseq-embeddings-pipeline` on the actual host; Nextflow (running inside the devcontainer) issues `docker run -v /workspaces/fisseq-embeddings-pipeline/work/...:...` to the host's daemon via the mounted socket, and the host daemon rejects it (`mounts denied: ... is not shared from the host and is not known to Docker`) because that literal path doesn't exist on the host — only its `/Users/gwen/Documents/...` counterpart does. This is a known class of friction with docker-outside-of-docker inside a devcontainer whose workspace folder is remapped to a different in-container path; not something fixable from inside this session (would need either a host-side path-translation setup or running Nextflow's containerized executor outside this specific devcontainer topology, e.g. directly on a Linux CI runner). `--gpus all`/`nvidia-smi` also remain unverified regardless — this devcontainer's host has no GPU.
- [x] `dinov2` vendoring/installation resolved inside the Dockerfile (currently a TODO in the scaffold — SPEC.md doesn't decide packaging strategy for it). Already resolved as of Epic 3 (`src/fisseq_embeddings_pipeline/vendor/dinov2/`, pure-torch source, no `xformers`/`cuml`/git-dependency needed — see that directory's `VENDORED_FROM.md`) but the `Dockerfile`'s own TODO comment never got removed to say so; removed now and replaced with a comment explaining dinov2 installs automatically as part of the package (confirmed via the scratch-directory `uv sync` + `import fisseq_embeddings_pipeline.vendor.dinov2...` check above).
- [x] Decide and document image versioning/publishing (registry, tag scheme, CI build) — explicitly out of scope in SPEC.md §9.2's Resolved note, needs a decision here. **Decided:** GitHub Container Registry (`ghcr.io/<owner>/<repo>`); `:latest` + `:<short-sha>` on every `master` push, `:<version>` additionally on a pushed `v*` tag; PRs build-only (no push, no credentials needed) as a Dockerfile-regression smoke test. Implemented in `.github/workflows/docker.yml`, full rationale in `docs/configuration.md`. Same "inert until this repo has a remote" situation as `.github/workflows/ci.yml` (AGENTS.md's Git workflow section) — added now, not run anywhere yet.
- [x] Revisit (not required for v1) whether a CPU-only image is worth splitting out, once real pull-time cost is measured. Revisited and explicitly deferred (documented in `docs/configuration.md`) — no real pull-time cost measurement is possible without a registry/real usage yet; SPEC.md §9.2 already flags this as non-blocking for v1.

---

## Epic 11 — Testing (SPEC.md §9.3)

### Story 11.1 — Unit test coverage
- [ ] One test module per stage exists (`test_dataset.py`, `test_qcfilter.py`, `test_embed.py`, `test_filter.py`, `test_aggregate.py`, `test_ovwt.py`, `test_global_embeddings.py`, `test_global_distinguishability.py`), mirroring `fisseq-data-pipeline/tests/unit/`'s one-module-per-stage convention.
- [ ] Every "Resolved" note across `SPEC.md` §6 that describes specific behavior (dropped downsampling, no per-barcode pooling, graceful null-on-zero-variance, etc.) has at least one test asserting that behavior, not just the happy path.

### Story 11.2 — Integration suite
- [ ] `tests/integration/test_integration.py` implements the session-scoped `pipeline_outputs` fixture (synthetic `phenotyping_dir` + experiment config, real `nextflow run` via `subprocess`).
- [ ] Synthetic fixture generator (`_write_synthetic_tiles`) produces valid tiny crops/masks/cell tables matching `starcall-workflow`'s `make_cell_images` output shape closely enough for `BUILD_DATASET` to ingest them without special-casing.
- [ ] `test_pipeline_exits_cleanly`, `test_filter_embeddings_has_no_embedding_columns`, `test_ovwt_results_have_both_auroc_columns` implemented per the scaffold's sketch.
- [ ] `test_rerunning_with_same_seed_reproduces_ovwt_scores` implemented for real (a stub `...` in the sketch) — this is the test that actually proves SPEC.md §3 decision 11's reproducibility claim, not just that the field exists.
- [ ] `EMBED_CELLS`' CI/GPU-checkpoint problem (SPEC.md §9.3's Resolved note) decided: tiny stub checkpoint vs. test-mode fake — implement whichever was chosen, and document the choice in `AGENTS.md`'s testing section.
- [ ] CI actually runs `tests/integration` (with `nextflow` installed) on some cadence, even if not every PR (GPU/nextflow-runtime cost permitting) — decide and document.

---

## Epic 12 — Documentation & release readiness

### Story 12.1 — Real docs, replacing the placeholders
- [ ] `docs/index.md`, `docs/architecture.md`, `docs/nextflow.md`, `docs/configuration.md` rewritten with real content (not the "see SPEC.md" placeholders this scaffold ships with), once the corresponding epics are done.
- [ ] `README.md`'s "Status: pre-implementation" note removed/updated once the pipeline actually runs end to end.
- [ ] `mkdocs` site builds and publishes (mirroring `fisseq-data-pipeline`'s `mkdocs.yml`/`site/` setup) if that's still the desired docs delivery mechanism.

### Story 12.2 — Housekeeping
- [ ] `LICENSE.txt` added (not yet decided — flagged in `README.md`).
- [ ] `.github/` CI workflows added (lint + unit tests at minimum; integration tests per Epic 11's cadence decision).
- [ ] `SPEC.md` reconciled against final implementation — any place implementation legitimately diverged from the spec (beyond the two items §10 always expected to change) should be called out in `docs/architecture.md`, not left as a silent mismatch between the two documents.
